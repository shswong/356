package observer;

/**An observer looks for things that it observes so that it can update
 * 
 *
 */
public interface Observer {
	
	/**Get new data that we know about
	 * 
	 */
	public void updateData();

}
